// it should have a list of all projects at start
// it should have a skill list
// it sho
// it should filter project's list when clicked at one skill
// it should redirect to project's url when clicked at the first project
